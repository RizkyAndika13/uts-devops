---
- name: Setup Data Science Environment
  hosts: server
  become: false

  gather_facts: false

  tasks:
    - name: Update repository dan install Python
      raw: apt-get update && apt-get install -y python3 python3-pip

    - name: Install pandas
      pip:
        name: pandas
        extra_args: --break-system-packages